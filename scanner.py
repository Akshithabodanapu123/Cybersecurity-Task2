Python Port Scanner

This project is a simple Python-based port scanner developed using the socket module. It scans a target IP address and checks whether ports are open or closed. The program attempts to establish a connection to ports within a specified range and displays all open ports found during the scan.

Features:

* Accepts a target IP address as input.
* Scans ports from 1 to 100.
* Detects and displays open ports.
* Uses Python's built-in socket library.
* Provides quick network reconnaissance results.

Sample Output:
Port 22 is OPEN
Port 80 is OPEN
Scan Completed!

Technologies Used:

* Python 3
* Socket Module
* Termux/Linux Environment
